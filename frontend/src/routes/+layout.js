// Static SPA — the app uses document, window, navigator.clipboard,
// document.title and the Page Visibility API, so SSR/prerender must be off.
export const ssr = false;
export const prerender = false;
